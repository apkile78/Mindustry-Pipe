{
  "name": "mindustry-pipe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "node scripts/patch-web-build.mjs",
    "build:legacy": "node scripts/patch-web-build.mjs --input legacy-web --out dist",
    "check": "node scripts/patch-web-build.mjs --input examples/legacy-web --out build/check-dist"
  }
}
